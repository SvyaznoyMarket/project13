<div id="popup" class="personal-order__modal js-modal">
    <div class="personal-order__modal">
        <div class="personal-order__modal-body">
            <header class="personal-order__modal-head">
                Отменить заказ?
            </header>

            <div class="personal-order__madal-name-item">
                {{order.numberErp}}
            </div>

            <form action="{{url}}" method="post">
                <input type="hidden" name="order[id]" value="{{order.id}}">
                <button type="submit" class="personal-order__modal-send">Отменить</button>
            </form>

            <a class="personal-order__modal-close js-modal-close"></a>
        </div>
    </div>
</div>


