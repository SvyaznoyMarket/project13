/**
 * @module      enter.config
 * @version     0.1
 *
 * [About YM Modules]{@link https://github.com/ymaps/modules}
 */
!function( modules, module ) {
    modules.define(
        'enter.config',
        [],
        module
    );
}(
    this.modules,
    function( provide ) {
        'use strict';

        var
            config = {};

        provide(config);
    }
);
