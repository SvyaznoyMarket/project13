modules.define(
    'enter.cart.common',
    ['enter.config'],
    function (provideModule, config) {
        var module = (function () {
            var init = function (config) {

            }

            return {
                init: init
            };
        }());

        provideModule(module);
    }
);