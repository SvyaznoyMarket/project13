modules.define(
    'enter.config',
    [],
    function (provide) {
        var provideModule = (function () {
            var init = function (config) {

            }

            return {
                init: init
            };
        }());

        provide(provideModule);
    }
);